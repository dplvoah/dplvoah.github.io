name: AI Comment On Post

on:
  workflow_dispatch:
    inputs:
      slug:
        description: "Blog post slug"
        required: true
        type: string

jobs:
  post-ai-comment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Debug current git revision
        run: |
          git rev-parse HEAD
          git branch --show-current

      - name: Debug markdown payload
        run: |
          echo "=== RAW FILE ==="
          cat "src/content/blog/${{ inputs.slug }}.md"
          echo
          echo "=== PARSED FILE ==="
          python scripts/load_markdown.py --slug "${{ inputs.slug }}" --pretty

      - name: Generate and publish AI comment
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          AI_GITHUB_TOKEN: ${{ secrets.AI_GITHUB_TOKEN }}
        run: |
          python scripts/post_ai_comment.py --slug "${{ inputs.slug }}" --write-file